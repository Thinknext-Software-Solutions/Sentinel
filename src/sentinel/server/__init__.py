"""Sentinel Studio: self-hosted multi-user team server.

Adds projects, runs, history, and user management on top of the CLI.
Single SQLite DB, FastAPI backend, React SPA frontend.

Install: pip install 'sentinel-agent[server]'
Bootstrap: sentinel server init   (creates admin)
Run:       sentinel server up     (starts http://localhost:8000)
"""
